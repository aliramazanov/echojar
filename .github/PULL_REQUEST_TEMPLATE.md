## What this changes

## Why

## How it was checked

- [ ] `mvn verify` passes
- [ ] If counting changed, `ConformanceFuzzIT` still passes
- [ ] If this fixes a bug, a test fails without the fix and passes with it
